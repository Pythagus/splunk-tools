# Application-level permissions

[]
access = read : [ * ], write : [ admin, sc_admin ]
export = system

# The following elements need to be exported for the commands to be working outside the app'.

[commands]
export = system
access = read : [ admin, sc_admin ], write : [ admin, sc_admin ]

[searchbnf]
export = system
access = read : [ * ], write : [ admin, sc_admin ]
